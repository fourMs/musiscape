name: Deploy documentation

on:
  push:
    branches: [main]
    paths: ["docs/**", "mkdocs.yml", "src/**", ".github/workflows/docs.yml"]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0  # full history for git-revision-date-localized
      - uses: actions/setup-python@v5
        with:
          python-version: "3.x"
      - run: pip install -e . mkdocs-material "mkdocstrings[python]" mkdocs-git-revision-date-localized-plugin
      # This repo's Pages source is "GitHub Actions", so gh-deploy pushes to
      # the gh-pages branch were never published (site was stale since July).
      # Deploy through the Pages artifact flow instead.
      - run: mkdocs build --clean
      - uses: actions/upload-pages-artifact@v3
        with:
          path: site

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
